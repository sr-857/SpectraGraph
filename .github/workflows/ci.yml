name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  sanity-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify repository structure
        run: |
          echo "SpectraGraph CI is configured. Add real build/test steps as needed."
